# RAG - 检索增强生成

让 AI 基于你的文档回答问题，而不是编造答案。

## 特性

- 文档导入（Markdown / PDF / 网页）
- 向量检索 + 语义匹配
- 回答带引用（citation）
- 可替换检索后端

## 流程

```
文档 → 切块 → 向量化 → 存入向量库
用户提问 → 向量化 → 检索相似块 → 注入上下文 → LLM 回答（带引用）
```

## 可用实现

| Adapter | 说明 | 配置值 |
|---------|------|--------|
| LlamaIndex | 功能完整的 RAG 框架 | `llamaindex` |

## 扩展

实现 `RetrievalPort` 接口 + 注册即可。
