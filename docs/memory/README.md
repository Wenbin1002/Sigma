# Memory

跨会话记忆。让 AI 记住用户偏好、背景信息和关键决策。

## 特性

- 自动提取值得记住的信息
- 语义检索相关记忆
- 用户可查看/删除记忆
- 上下文压缩（长对话自动摘要）

## 可用实现

| Adapter | 说明 | 配置值 |
|---------|------|--------|
| Local | SQLite + 向量索引 | `local` |

## 扩展

实现 `MemoryPort` 接口 + 注册即可。
